# Core
# rebrowser-playwright removed: using Camoufox (C++-level fingerprint injection)
# which connects via standard playwright WS protocol. rebrowser patches only needed
# for local Chromium CDP, and they lag behind playwright versions causing WS mismatches.
beautifulsoup4>=4.12.0
httpx>=0.25.0
aiohttp>=3.9.0
requests>=2.28.0
pyyaml>=6.0
python-dotenv>=1.0.0

# LLM clients
anthropic>=0.3.0
openai>=1.0.0

# HTML parsing
parsel>=1.8.0
lxml>=4.9.0
pyquery>=2.0.0
selectolax>=0.3.0
html5lib>=1.1

# Data
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
pypdf>=4.0.0

# Database (optional — graceful degradation when DATABASE_URL not set)
asyncpg>=0.29.0

# Network
chardet>=5.0.0
fake-useragent>=1.4.0
Pillow>=10.0.0

# Dev
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
